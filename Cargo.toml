[package]
name = "cuda-confidence"
version = "0.1.0"
edition = "2021"
description = "Confidence primitive — the foundational type that makes every value uncertain and every computation honest"

[dependencies]
serde = { version = "1", features = ["derive"] }
